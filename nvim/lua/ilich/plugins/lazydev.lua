return {
  -- Configures lua_ls for Neovim config editing: loads only the modules
  -- you actually use instead of the whole runtime (much faster)
  "folke/lazydev.nvim",
  ft = "lua",
  opts = {},
}
